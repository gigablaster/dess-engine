image: ubuntu/oldlts
packages:
  - rust-all
  - pkg-config
sources:
  - https://git.sr.ht/~gigablaster/dess-engine
tasks:
  - build: |
      cd dess-engine
      cargo build
  - test: |
      cd dess-engine
      cargo test
  - clippy: |
      cd dess-engine
      cargo clippy
