image: ubuntu/oldlts
packages:
  - rust-all
  - cmake
  - libsdl2-dev
sources:
  - https://git.sr.ht/~gigablaster/dess-engine
tasks:
  - build: |
      cd dess-engine
      cargo build
  - test: |
      cd dess-engine
      cargo test
