image: ubuntu/oldlts
packages:
  - rust-all
sources:
  - https://git.sr.ht/~gigablaster/dess-engine
tasks:
  - build: |
      cd dess-engine
      cargo build
  - test: |
      cd dess-engine
      cargo test
